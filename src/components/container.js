import React from "react";

// I got this from the tutorial! Seemed like a useful div to contain everything.
export default ({ children }) => (
	<div style={{ margin: "3rem auto", maxWidth: 800 }}>{children}</div>
);